name: build-test-deploy
# on: push
on: [workflow_dispatch]
jobs:
  # `build` is the name of this job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v6
      - name: use node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20.x"
      - run: npm install
      - run: npm run build

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v6
      - name: use node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20.x"
      - run: npm install
      - run: npm test

  deploy:
    needs: test
    # Github token permissions
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: production
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: use node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20.x"
      - name: configure github pages
        uses: actions/configure-pages@v5
        with:
          static_site_generator: next
      - run: npm install
      - run: npm run build
      - name: upload artifacts
        uses: actions/upload-pages-artifact@v4
        with:
          path: "./out"
      - name: deploy
        # we used this identifier to grab the URL for our site with url:
        # ${{ steps.deployment.outputs.page_url }}
        id: deployment
        uses: actions/deploy-pages@v5
